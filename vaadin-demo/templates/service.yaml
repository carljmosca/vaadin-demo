apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaadin-demo.fullname" . }}
  labels:
    {{- include "vaadin-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vaadin-demo.selectorLabels" . | nindent 4 }}
